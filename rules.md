## 🪩 Правила добавления кода в проект

0. Создать `Project` для курсача.
1. Все задачки разделить на более мелкие и добавить в `Project`.
2. Выбрать себе задачку, отметить в `Project` что она в разработке.
3. Создать отдельную ветку для задачки с названием:
`название папки` - `глагол типо add` - `название что делаем`.
Например: `backend-add-api` или `ml-fix-training-pipeline`.
4. После того как все сделали пушим и создаем `PR` с красивым названием и описанием (есть шаблон).
5. Добавляем друг друга в `Reviewers` на всякий или есть `CODEOWNERS`.
6. Если все ок, то мерджим и отмечаем задачку как выполненную в `Project`.
7. Удаляем отдельную ветку.
8. Переходим на ветку `main` и **подтягиваем** все новые изменения!
9. Делаем все по новой с пункта 2 (или 1).
10. Кайфуем 😋

---

<div align="center">
  <img src="https://i.pinimg.com/1200x/3e/95/ea/3e95ea9fe26b6532ab129effcf15c6ed.jpg" width="90%">
</div>

<div align="center">
  <img src="https://i.pinimg.com/1200x/eb/e8/50/ebe850381dab5db731d38988aabdc656.jpg" width="90%">
</div>

---

## 🥂 Git команды
- `git status` - статус репозитория
- `git add .` - добавить изменения
- `git commit -m "..."` - закоммитить изменения
- `git push -u origin ...` - запушить изменения
- `git checkout ...` - переместиться на другую ветку
- `git checkout -b new-branch-name` - создать новую ветку и переместиться на нее
- `git pull origin ... ` - подтянуть удаленные изменения

#### Добавить себе в ветку изменения из `main`:

```bash
git checkout ... # переключаемся на нашу ветку

git fetch origin # получаем последние изменения из удаленного репозитория

git merge origin/main # мерджим изменения из main в нашу ветку
```

#### Рещение конфликтов при `merge`:
1. Открываем файлы с конфликтами и разрешаем их
2. Добавляем исправленные файлы `git add ...`
3. Продолжаем процесс `git merge --continue`

---

## 💖 Работа с `Project`
- Добавить `Label`
- Добавить исполнителя `Assigner`
- Добавить ветку с задачей `branch`
- Когда начинаем работать над задачкой поставить `Start date` (особо не надо, но прикольно)
- Когда начинаем работать над задачкой поставить `Target date` (тоже прикольно)
- Когда заканчиваем работать над задачкой добавляем `Linked pull request`
