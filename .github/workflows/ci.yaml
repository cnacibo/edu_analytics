name: CI (minimal)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

#      - name: Cache pip dependencies
#        uses: actions/cache@v4
#        with:
#          path: ~/.cache/pip
#          key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}
#          restore-keys: |
#            ${{ runner.os }}-pip-
#
#      - name: Install dependencies
#        run: |
#          python -m pip install --upgrade pip
#          pip install ".[backend,ml,parser,dev]"
#
#      - name: Run linters
#        run: |
#          mkdir -p reports
#          echo "Running Ruff..."
#          ruff check . --output-file=reports/ruff.txt || echo "Ruff check completed"
#          echo "Running Black..."
#          black --check . 2>&1 | tee reports/black.txt || echo "Black check completed"
#          echo "Running Isort..."
#          isort --check-only . 2>&1 | tee reports/isort.txt || echo "Isort check completed"
#
#      - name: Run tests
#        run: |
#          pytest -q --junitxml=reports/junit.xml --tb=short
#
#      - name: Upload test reports
#        uses: actions/upload-artifact@v4
#        with:
#          name: test-reports
#          path: reports/
#          retention-days: 30
